# ===========================================
# IntroLink Backend Configuration
# ===========================================

# MongoDB Atlas Connection String (REQUIRED)
# 1. Create a free cluster at https://cloud.mongodb.com
# 2. Go to Database Access and create a user
# 3. Go to Network Access and add your IP (or 0.0.0.0/0 for development)
# 4. Click "Connect" on your cluster and get the connection string
# Example: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/introlink?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/introlink?retryWrites=true&w=majority

# Server Port
PORT=3001

# ===========================================
# API Keys for Data Providers
# ===========================================

# Apify API Token for People & Job Search
# Get your token from: https://console.apify.com/account/integrations
APIFY_TOKEN=

# Exa API Key for People Search (used by Apify actor)
# Get your key from: https://exa.ai
EXA_API_KEY=

# Happenstance AI API Key for Person Enrichment (email lookup, contact info)
# Get your key from: https://happenstance.ai
# Used to enrich people search results with email addresses and personal information
HAPPENSTANCE_API_KEY=

# Tomba API Credentials for LinkedIn Email Lookup
# Get your credentials from: https://tomba.io/
# Used to find email addresses from LinkedIn profile URLs
TOMBA_API_KEY=
TOMBA_API_SECRET=

# ===========================================
# Email Sending Configuration (SMTP)
# ===========================================

# SMTP Configuration for sending emails
# You can use Gmail, SendGrid, Mailgun, or any SMTP provider
#
# For Gmail:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASS=your-app-password (generate at https://myaccount.google.com/apppasswords)
#
# For SendGrid:
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_PORT=587
#   SMTP_USER=apikey
#   SMTP_PASS=your-sendgrid-api-key
#
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=IntroLink
SMTP_FROM_EMAIL=

# Your name for signing off emails (appears after "Best," or "Thanks,")
SENDER_NAME=Bala

# ===========================================
# AI/LLM API Keys (for Agentic Workflow)
# ===========================================

# Fireworks AI API Key (required for agentic workflow)
# Used for:
#   - Resume parsing and extraction
#   - Personalized email generation
# Get your key from: https://fireworks.ai/account/api-keys
FIREWORKS_API_KEY=

# ===========================================
# Agentic Workflow Configuration (Optional)
# ===========================================

# Maximum jobs to fetch per role (default: 10)
# AGENT_MAX_JOBS_PER_ROLE=10

# Maximum contacts to find per job (default: 3)
# AGENT_MAX_CONTACTS_PER_JOB=3

# Delay between API calls in ms (default: 1000)
# AGENT_DELAY_MS=1000
